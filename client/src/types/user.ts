export type UserType = {
  jwt: string;
  username: string;
  image: string;
  questionIndex: number;
  questionAnswer: string;
  email: string;
};
export type AuthType = {
  email: string;
  password: string;
};
